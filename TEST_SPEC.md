# TEST_SPEC.md — physics-puzzle-lab

<!-- scaffold template v1.30.0 から展開(2026-09-08)。ランナーを vitest に読み替えて運用する -->

## 実行規約

- `npm test`(`vitest run`)を stage 3–5 の判定に使用する。E2E は `npm run test:e2e`(Playwright)
- テストは `tests/` 配下。`tests/physics/` `tests/game/` `tests/meta/` `tests/e2e/`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(初期条件・無損失性・単一物体等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 原仕様(『Physics Puzzle Lab V1.0 完全実装仕様書』) | 節番号を書く |
| 外部権威(物理法則・公表値) | 法則名と適用条件を書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。

**閾値は測る前に書かない**(HC-135)。未実測の閾値は必ず赤になる値(0 等)を置くか、
数値を書かず「実測後に置き直す」と明記する。もっともらしい数値を仮置きしてはならない。

## オラクルの出所

| フィクスチャ / オラクル | 出所 | 性格 |
|---|---|---|
| ステージ同梱の解答(`solution`) | 本プロジェクトが作り、出荷エンジンで到達を確かめる | **片側健全**(解けると言えた面だけを配る。解けないとは言えない) |
| 二回実行の一致 | 同一入力・同一シード | 決定論。**同一 JS エンジン内に限る**(SPEC D-02) |
| 力学的エネルギー保存 | 外部権威(古典力学の保存則)。エンジンの積分則を再実装しないので循環しない | 近似。閾値は実測後 |
| 自由落下の閉形式 `y = y0 + ½gt²` | 外部権威(等加速度運動) | 近似。半陰的 Euler は閉形式と系統的にずれるので、**一致ではなく境界**で書く |
| ステージ JSON 検証の陽性/陰性対照 | 本プロジェクトが作る不正値・正常値の対 | 検査器そのものを検査する |

## ケース一覧

**「対応要求」には SPEC の ID を必ず書く**(HC-157)。品質ゲート `G-xx` を確かめるケースには
その `G-xx` を書くこと。どのケースからも参照されない `G-xx` は、SPEC 側に「未実装」と
明記するか、ケースを書くかのどちらかにする。

### L0 — 骨組み

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | G-00 | SPEC の全 `G-xx` が、TEST_SPEC のケース表から参照されるか SPEC 側で「未実装」と明記されている | 例外 0 件 |
| T-002 | G-00 | T-001 の陽性対照 — 参照も未実装表記も無いゲートを混ぜた入力で、検査が実際に落ちる | 落ちる |
| T-003 | F-01 | 論理キャンバス定数が SPEC D-04(900×540)と一致する | 一致 |

### L1 — 物理コア

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-010 | F-02 / G-02 | 同一ステージ・同一配置で二回実行し、全 tick の位置・速度・角度を比べる。衝突を含む盤面を使い、経路(累計衝突回数の推移)も比べる | 全 tick でビット単位に一致 |
| T-011 | G-02 | T-010 の陽性対照 — 初期位置を 1e-12 ずらす / 板を 10 px 動かす | どちらも軌跡が食い違う |
| T-012 | G-02 | Slow Motion(`timeScale`)を変えて実行する | 軌跡は変わらない(SPEC D-07) |
| T-013 | G-04 | matter-js のソースから導いた実効加速度と、自由落下で観測される加速度を突き合わせる。`gravityScale` の既定値も固定する | 一致(1e-10 の粒度)。仮定が崩れたら落ちる(HC-075) |
| T-014 | G-04 | 空気抵抗 0・接触 0 件の自由飛行 50 tick で力学的エネルギーの相対変動を測る | 実測 0.0142 に対し閾値 0.03。残差は半 tick の帳尻であって散逸ではない |
| T-015 | G-04 | T-014 の陽性対照 — 空気抵抗を入れた世界と比べる | 散逸側がはっきり大きく減る(実測 約 12.6 倍、閾値 4 倍) |
| T-016 | F-04 | 板の有無で同じ tick 数だけ走らせる | 板がある側だけ受け止められる |
| T-017 | F-05 | 動的な箱と静的な箱を落とす | 動的は落ち、静的は 1 px も動かない |
| T-018 | F-06 | 自然長より 120 px 下からばねで吊るす | 床に達さず、20 px 以上戻る点がある |
| T-019 | F-07 | 送風の範囲内・範囲外・向きの反転を比べる | 範囲内だけが押され、向きを反転すると押される向きも反転する |
| T-020 | F-09 | プレイヤーの配置を渡して実行する | 配置した板が実際にボールを受け止める |

### L2 — ゲーム層

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-021 | F-12 / G-05 | **陰性対照を先に置く**(HC-074)。正常なステージ 3 種(最小・全部品入り・境界値ちょうど)を検証器に通す | 誤検出 0 件 |
| T-022 | F-12 / G-05 | 不正値 14 種(座標のはみ出し・friction 1 超・restitution 負・gravity 5 超・半径 0・timeLimit 0・ツール個数が負・ランク順序の破れ・id 重複・ばねの接続先不在・ゴールのはみ出し・range 負)を 1 件ずつ入れる | それぞれ該当する path が報告される |
| T-023 | G-05 | 検証器そのものの検査 — 1 箇所だけ壊したときに報告が 1 件であること、複数壊したときに全件報告されること | 「何でも弾く」検査器でないこと・最初の 1 件で止まらないこと |
| T-024 | F-14 / G-06 | スコアの内訳と合計を原仕様 §23 の式と突き合わせる。下限 0・経過秒の切り捨て(SPEC D-09)も見る | 式どおり |
| T-025 | F-15 / G-06 | ランク境界を 8 点(1000 / 900 / 899 / 700 / 699 / 400 / 399 / 0)で確かめる。ステージ固有のしきい値でも見る | §24 の帯どおり |
| T-026 | F-08 / G-07 | ゴールの真上からボールを落として通過させる。滞在 tick の推移を記録する | 滞在は 1 以上 18 未満に収まり、CLEAR にならない。ゴールを出たら 0 に戻る |
| T-027 | F-08 / G-07 | 同じ配置にゴール内で止まる板を足す | CLEAR になり、滞在が 18 tick 以上 |
| T-028 | G-07 | しきい値の定数が 300 ms(= 18 tick)と対応する | 18 |
| T-029 | F-09 | ツールの個数制限 — 上限まで置ける / 超えると例外 / 個数 0 は置けない / 削除で枠が戻る / 使用数がスコアの usedObjects と一致 | 原仕様 §20 どおり |

**T-026〜T-028 は赤を見ていない**(HC-230)。G-07 の判定は L1 の `Simulation.step()` に
既に入っていたため、テストは緑で到着した。判別性は赤→緑の遷移ではなく、
**滞在 tick が 0 と 18 の間に挟まること**で示している —— 通過は必ず 1 tick 以上入り、
かつ 18 tick には届かないので、しきい値がどちらへずれてもこの検査は落ちる。

### L3 — ステージ機構(規定運動・リンク・複数ボール)

原仕様 §22 の Stage 13 / 14 / 15 / 17 / 18 は、これらが無いと作れない。

**閉じた式を期待値に書かない**(HC-045)。`base + A·sin(2π(t/P+φ))` で実装して
同じ式を期待値にすれば、一致はほぼ恒等式になり緑でも何も検査していない。
だから式ではなく**性質**(範囲・周期・到達・再現)で書く。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-030 | F-13 / D-10 | 上下振動が基準 ±振幅 に収まる / 実際に上端下端の近くまで届く / 1 周期後に基準へ戻る | 三つとも成立。**各ケースで `status` が RUNNING** であることも確かめる |
| T-031 | F-13 / D-10 | 回転する床の角度が単調増加し、1 周期で 2π 進む | 成立 |
| T-032 | F-13 / D-10 | 動くゴールが実際に動く | 振れ幅 > 250 px |
| T-033 | F-13 / D-10 | 振動する台に載ったボールが持ち上がる | 50 px 以上上がる(`updateVelocity = true` を選んだ理由) |
| T-034 | F-13 / D-11 | 追加ボールが物理で動き主ボールと衝突する / **ゴール内に追加ボールを置いても CLEAR にならない** | 判定は主ボールだけ |
| T-035 | F-13 | リンクで繋いだ箱が、繋がない対照より落ちない | 成立 |
| T-036 | G-02 | mover・link・複数ボールを含む世界で二回実行が一致する | 全 tick でビット一致 |
| T-021 追補 | G-05 | 規定運動・リンク・複数ボールを含む正常ステージを弾かない | 誤検出 0 件 |
| T-022 追補 | G-05 | 規定運動の対象不在 / 周期 0 / 振幅が負 / リンクの接続先不在 / stiffness 範囲外 / 追加ボールのはみ出し・半径 0 | それぞれ該当 path が報告される |

**T-030〜T-032 は一度、凍結値を測っていた**(HC-234)。ボールが世界の外へ落ちて
`status=FAILED` になり `step()` が早期 return し、台が tick 114 の位置で止まっていた。
**停止した系と静止した系は値だけでは区別がつかない**ので、範囲の検査は凍結していても緑になる。
捕まえたのは対で置いた「実際に端まで届く」だけだった。以後、測定面には床を敷き、
各ケースの末尾で `status` を確かめる。

### L4 — 20 面と解答可能性

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-040 | F-13 / G-01 | 収録 20 面それぞれについて、同梱解答を**出荷エンジン**で無頭実行する | 全面が制限時間内に CLEAR。解答が使う道具数は各面の上限以内 |
| T-041 | G-01 | 解答可能性検査そのものの検査 — (a) 道具を置かなければクリアできない面が 10 面以上ある (b) 解答を (160, 90) ずらすと 10 面以上が解けなくなる | 「何でも CLEAR」になっていないこと・配置が効いていること |
| T-042 | F-12 / G-05 | 収録全面が `validateStage` を通り、`loadStage` が全面を読める。存在しない id は例外 | 誤検出 0 件 |
| T-043 | N-02 / G-09 | 解答を置いた状態(物体が最も多い状態)で Bodies / Constraints を数える | Bodies ≤ 100 / Constraints ≤ 30 |
| T-044 | F-13 | 名前・テーマ・ヒントが全面にある。難易度が 1〜5 で、後半 10 面の平均が前半より高い | 成立 |

**解答は探索で見つけ、出荷エンジンで検証してから同梱している。** 探索器(`tests/helpers/solve.ts`)は
出荷物ではなく、面を作る側の道具である(原仕様 §5 が対象外とした AI Solver は
遊ぶ人に解を見せる機能のことで、これとは別)。

**探索の候補は保存する粒度(小数 2 桁)に丸めてから試す。** 丸めを後段に置くと、
検証した配置と面データに書く配置が別物になる —— G-02 で 1e-12 の摂動が軌跡を変えることを
実証しているので、0.005 の丸めは十分に結果を変える。実際 stage11 / 15 / 20 で
探索は CLEAR、保存解答は FAILED になった(VERIF-FALSE、loop_004)。

### L5 — リプレイ・保存・進行

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-050 | F-18 / G-03 | CREATE / MOVE / ROTATE / DELETE を畳んで最終配置を復元する。復元結果がその面の同梱解答と一致することも見る | 消した物は残らず、最後の MOVE / ROTATE が効く |
| T-051 | F-18 / G-03 | 再生した結果と、復元配置をそのまま走らせた結果を突き合わせる。RESET はリセット回数に数える | 状態・経過 tick・スコアが一致 |
| T-052 | G-03 | リプレイ検査そのものの検査 — ログを 1 件削る / 配置を 1 px ずらす | どちらも結果が変わる |
| T-053 | F-17 / G-08 | 保存と復元の往復。空の保存領域・壊れた保存内容も見る | 往復して一致。壊れていたら既定値へ落ち、例外にしない |
| T-054 | F-16 / G-08 | Stage N のクリアで N+1 が開く。解放は戻らない。ベストスコアは高いほうだけ残る。§39 の表示状態 | 原仕様 §38 / §39 どおり |
| T-055 | N-04 / G-11 | 書き込むキーを数える。**書き出した内容の鍵も数える** | キーは `physicsPuzzleLab.v1` のみ。項目は unlockedStage / bestScores / settings のみ |
| T-056 | F-11 | READY → EDITING → RUNNING の遷移。Pause 中は時間が進まない | 原仕様 §36 どおり |
| T-057 | F-11 | Reset で時間が戻り回数が増える。**配置は残る**。回数がスコアに効く | ResetPenalty = 回数 × 30 |
| T-058 | F-11 / D-07 | Slow Motion を掛けても、同じ tick 数進めた結果は等速と一致する。変わるのは 1 フレームあたりの tick 数のほう | 物理は変わらない |
| T-059 | F-09 | ツール上限に達したら `place` が拒否される。削除で枠が戻る | 例外になる |

**G-11 は「個人情報を保存しない」を宣言ではなく鍵の数で守る。** 書き出した JSON の
トップレベルと `settings` の鍵を固定してあるので、項目を増やせばテストが落ちる。

### L6 — 画面と公開前検査

#### 単体(vitest)

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-060 | F-01 | 描画モデルにボール・ゴール・固定物・配置がすべて含まれる。座標が物理の状態と一致し、動けば図形も動く。多角形は頂点で持つ | 外枠は描かない。角度と頂点を二重に持たない |
| T-061 | F-27 / F-29 | 画面座標と論理座標が 4 つの幅で往復する。拡大には上限がある | 往復して元に戻る |
| T-062 | F-10 | 掴めるのは置いた部品だけ。重なりは後から置いたものが優先 | 固定物とボールは掴めない |
| T-063 | F-28 | キー割り当てが原仕様 §45 と一致。入力欄では横取りしない | 割り当ての無いキーは何も起こさない |
| T-064 | N-03 / N-05 / G-10 | 出荷される木を走査し、外部素材の読み込み・外部通信・`dangerouslySetInnerHTML` を探す。**陽性対照つき**(捕まえるべき悪い例 3 種と、撃ってはならない正当な例 3 種) | 違反 0 件。対照は撃つ／撃たない |
| T-065 | N-06 / G-12 | タップ領域 44px 以上が押せる要素 4 種の CSS にある。`prefers-reduced-motion` に応答する。Canvas に `role` と `aria-label` がある | すべて満たす |
| T-066 | — | フッタの 3 項目がレイアウトにあり、LICENSE ファイルが実在する | 実在する |
| T-067 | N-07 / G-13 | `python harness/text_hygiene.py` を検証段と CI(loop-verify)で走らせ、日本語本文への別字種・制御文字の混入を検出する | 違反 0 件。走査対象が空でないことは出力の走査ファイル数で確かめる |

#### 実ブラウザ(Playwright / G-14)

**在ることではなく、幾何と到達を測る**(HC-138)。座標を使う操作は、
**操作が届いた証拠**(状態の変化)を確かめてから結果を判定する。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| E-01 | F-23 / G-14 | ホーム → ステージ一覧 → ゲーム画面へ到達する。20 面が並び、初期状態は NEW 1 面 / LOCKED 19 面 | 原仕様 §38 どおり |
| E-02 | F-01 / G-14 | Canvas の**境界矩形**を測り、画素を標本して色数を数える | 潰れていない。単色でない(= 実際に描かれている) |
| E-03 | F-16 / G-14 | Stage 01 を Start して最後まで走らせる。ランクが出て、一覧の LOCKED が 1 つ減る | クリアと解放が実際に起きる |
| E-04 | F-09 / G-14 | 盤面を押して部品を置き、**残数の変化**で操作が届いたと判定する | 2/2 → 1/2 |
| E-05 | F-28 / G-14 | Space で開始・一時停止、R でリセット | 状態表示が変わる |
| E-06 | F-27 / G-14 | **3 つの幅**(375 / 768 / 1280)で 3 画面の横の溢れと縦の伸びすぎを測る | 溢れ ≤ 1px、高さ < 16,000px |
| E-07 | G-14 | 溢れ検査そのものの検査 — 幅 3000px の要素を差し込む | 検査が実際に撃つ |

#### 目視検品(`tests/e2e/inspect.spec.ts` / HC-041・HC-250)

撮影は合否を判定しない。**機械の代理指標では捕まらないもの**だけを人間が見る:

1. 狭い幅で **Canvas → TOOLS → 操作ボタン** の並びか(原仕様 §43)
2. 盤面の部品が見分けられるか
3. 残数・状態・ランクが**色以外**でも読み取れるか
4. フッタが本文に重なっていないか

**代理指標と目視は直交する**(HC-250)。loop_006 では、横の溢れ(375px で 545px)は
機械が捕まえ、並びの誤り(TOOLS が最下部)は撮った画像を見て初めて出た。
どちらも他方の方法では見つからなかった。`fullPage` は使わない —— 固定フッタが
途中へ焼き込まれて偽の異常が出る。

### L7 — Lab Mode・文章のページ・音

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-070 | F-19 | スライダーが原仕様 §32 の 10 項目を覆う。既定値が範囲に収まる。**端どうしで作った世界がステージ検証を通る** | 端まで動かしても壊れた世界を作れない |
| T-071 | F-20 | プリセット 7 種の値が原仕様 §18 / §33 と一致する。**触れていない項目を書き換えない**。知らない名前は既定へ落ちる | 例外にしない |
| T-072 | F-21 | 状態量が原仕様 §34 の 8 項目を覆い、値が物理の状態からそのまま取られる | 画面が独自に計算していない |
| T-073 | F-22 | 速度ベクトルが速度の向きを指し、速いほど長く、上限がある。**止まっていれば出さない** | 長さ 0 の矢印は向きが不定なので描かない |

#### 実ブラウザ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| E-10 | F-21 / G-14 | Lab の実験台が描かれ、状態量が実際に動く | 8 項目・値が変化する |
| E-11 | F-20 / G-14 | Zero Gravity を当てると縦の速度が増えなくなる | 値が物理に届いている |
| E-12 | F-20 / G-14 | Moon と Heavy World の落下速度を比べる。**落下中であることを assert で固定してから測る** | Heavy が Moon の 2 倍超 |
| E-13 | F-19 / G-14 | スライダーが 10 本ある | 原仕様 §32 |
| E-14 | F-24 / G-14 | About / 遊び方 / ライセンスが読め、要点の文言を含む | 「SI 単位ではない」を含む |
| E-15 | F-24 / G-14 | ホームから 4 ページへ到達できる | 原仕様 §40 |
| E-16 | F-27 / G-14 | 新しい 4 ページを 3 つの幅で測る | 溢れ ≤ 1px |

**E-12 は一度、着地後の速度を測っていた**(VERIF-FALSE / HC-234)。実時間 900ms 後に測ると、
重力 2.0 では既に実験台へ着いていて速度が 0 に戻っており、「重いほうが遅い」という
もっともらしい数が出た。**測定窓の途中で系が別の状態(落下 → 静止)へ移った**のに、
読み出しは平然と数を返し続ける。窓を 250ms に縮め、
**まだ落下中であること**(Position Y < 340)を assert で固定して直した。

### L8 — 公開と本番検品

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| P-01 | F-30 / G-14 | 本番の `/` と `/about` に、**本アプリにしか無い一続きの文**(「物理で遊ぶ。実験して理解する。」「配る面は、解けることを確かめてから配っている」)が出る | 2/2 合格。本番に当てるときは**これを最初に単独で**走らせ、落ちたら他を見ない |

**なぜ同一性を先に見るか。** 初回デプロイで恐いのは古さではなく**取り違え**である。
別名が他人のアプリを指していても 200 が返り、健やかさの検品は全部緑になる
(メモリ: `app-menu.vercel.app` は他者の「White Label App」だった)。

**同一性の陽性対照**: 別のアプリ `app-menu-amber.vercel.app` に当てると P-01 が落ちる
(実測 2026-09-14: exit 1)。落ちない検査は、配っている物を何も確かめていない。

**本番に対する全検品**: `PROD_URL=https://physics-puzzle-lab-gamma.vercel.app` で
E-01〜E-16 と P-01 を当て **22/22 合格**(2026-09-14、CLI の初回デプロイに対して)。
撮影(`inspect.spec.ts`)は除く —— 画像の保存先がプロセスの cwd に依存するため。

**検品したあと本番が差し替わった。** CLI の初回デプロイは GitHub リポジトリとの連携を
自動で張っており、次の push(`c52d83c`)で本番が Git 連携の自動デプロイに置き換わった。
アプリのコードは変わっていないが、「本番を検品した」を**いま配っている物**について
真に保つため、P-01 を当て直した(2/2 合格)。健やかさの検品は
「新しいか」「同じものか」を答えない(HC-148)。

**P-01 の ID は G-00 の被覆検査に数えられない**(`lib/specGates.ts` は `T-` と `E-` だけを拾う)。
P-01 が担う G-14 は E-01〜E-16 が既に参照しているので被覆に穴は無いが、
本番用のケースを増やすときは ID の系統を検査器に足すこと。
