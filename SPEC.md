# SPEC.md — physics-puzzle-lab(Physics Puzzle Lab V1.0)

<!-- scaffold template v1.30.0 から展開(2026-09-08)。以後このファイルはプロジェクトが育てる -->

原典は『Physics Puzzle Lab V1.0 完全実装仕様書』(以下「原仕様」、節番号は原仕様のもの)。
本 SPEC は原仕様を実装可能な粒度へ落とし、検査にトレースできる ID を与えたものである。

## 1. 目的

2D 物理シミュレーション上でボールをゴールへ導くブラウザゲームと、物理パラメータを
自由に動かせる実験室(Lab Mode)を、**課金経路ゼロ**(バックエンド・DB・外部 API なし)で
公開する。遊びと実験と教育を同じ物理エンジンの上で行き来させることを狙う。

**この応用に固有の難しさは「物理が正しいことをどう言うか」ではなく、
「配ったステージが本当に解けることをどう言うか」である。** 物理エンジンは近似であり、
SI 単位の再現は目的ではない(原仕様 §18)。したがって本プロジェクトの背骨は
**解答可能性の証明**(G-01)と**決定論**(G-02)に置く。

## 2. 機能要求

原仕様 §71「Definition of Done」を要求 ID 化したもの。

| ID | 要求 | 原仕様 | 優先度 |
|---|---|---|---|
| F-01 | Next.js アプリが起動し、Canvas にゲーム画面を描画する | §6, §9 | must |
| F-02 | Matter.js による 2D 物理シミュレーションが動作する | §7.4, §48 | must |
| F-03 | Ball が重力で落下する | §10, §49 | must |
| F-04 | Board(板)を配置でき、Ball と衝突する | §11, §50 | must |
| F-05 | Block(箱)が物理挙動する | §12 | must |
| F-06 | Spring(ばね)が Constraint として動作する | §13 | must |
| F-07 | Fan(送風)が範囲内の物体へ力を加える | §14, §52 | must |
| F-08 | Goal を Sensor として実装し、滞在 300 ms 以上で CLEAR とする | §15, §16, §51 | must |
| F-09 | Tool Palette(BOARD/BLOCK/SPRING/FAN)から部品を配置できる | §19 | must |
| F-10 | 配置した部品をドラッグ移動・回転・削除できる | §19, §44 | must |
| F-11 | Start / Pause / Reset / Slow Motion を操作できる | §28, §29 | must |
| F-12 | ステージ定義 JSON を読み込み、検証し、世界を構築する | §20, §54, §55 | must |
| F-13 | 20 ステージを収録する | §22 | must |
| F-14 | Physics Score を算出する(基準点 − 時間 − 部品 − リセット) | §23 | must |
| F-15 | GOLD / SILVER / BRONZE / CLEAR のランクを表示する | §24 | must |
| F-16 | クリア時に次ステージがアンロックされる | §38 | must |
| F-17 | 進捗・ベストスコア・設定を LocalStorage へ保存する | §37 | must |
| F-18 | 操作ログ方式の Replay を記録・再生できる | §26, §27 | must |
| F-19 | Lab Mode で物理パラメータを実時間で変更できる | §30, §31, §32 | must |
| F-20 | Lab プリセット(Earth/Moon/Mars/Zero-G/Ice/Bouncy/Heavy)を切り替えられる | §33 | must |
| F-21 | Lab で物体の実時間の状態量を表示する | §34 | must |
| F-22 | Lab で速度ベクトルを可視化する | §35 | should |
| F-23 | ステージ選択画面で状態(GOLD/SILVER/CLEAR/NEW/LOCKED)を表示する | §39 | must |
| F-24 | ホーム・遊び方・About・ライセンスの各ページを持つ | §8, §40 | must |
| F-25 | クリア演出(パーティクル)を Canvas で描画する | §42 | should |
| F-26 | Web Audio API による効果音(既定 OFF、ON/OFF 切替つき) | §41 | should |
| F-27 | レスポンシブ UI(デスクトップ / スマートフォン) | §43 | must |
| F-28 | キーボード操作(SPACE/R/DELETE/ESC/S) | §45 | must |
| F-29 | タップ・ドラッグによるタッチ操作 | §44 | must |
| F-30 | GitHub 公開と Vercel 本番デプロイ | §61, §62 | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 描画 60 FPS を目標とする。物理更新と React 再描画を分離する | 実ブラウザ検品で実測(L6) |
| N-02 | 1 ステージあたり Bodies ≤ 100 / Constraints ≤ 30 | ステージ JSON 全件の静的計数(G-09) |
| N-03 | 外部素材・CDN・外部通信を持たない(描画は Canvas と CSS、音は Web Audio 生成) | 依存とネットワークの静的検査(G-10) |
| N-04 | 個人情報を保存しない。保存先は LocalStorage のみ | 保存キーの検査(G-11) |
| N-05 | `dangerouslySetInnerHTML` を使わない | 静的検査(G-10) |
| N-06 | アクセシビリティ: aria-label / 色以外の状態表現 / 44px 以上のタップ領域 / `prefers-reduced-motion` | 実ブラウザ検品 + 静的検査(G-12) |
| N-07 | 日本語本文に別字種・制御文字を混入させない | `harness/text_hygiene.py`(G-13) |

## 4. 設計判断(原仕様からの逸脱と、その理由)

原仕様の記述と実装が食い違う箇所は、ここに理由とともに残す。**黙って変えない。**

| ID | 判断 | 理由 |
|---|---|---|
| D-01 | `Matter.Runner.run` を使わず、**固定タイムステップ**(1000/60 ms)で `Engine.update` を自分で回す | Runner は実時間 delta で更新するため、同じ操作が同じ結果にならない。原仕様は Replay(§26)・スコア再現(§23)・物理テスト(§58)を要求しており、可変 delta ではどれも成立しない。原仕様 §29 が「Runner 停止で実装する」を許容しているため、逸脱ではなく選択の範囲と解する |
| D-02 | 決定論の主張範囲は**同一 JS エンジン内**に限る。ブラウザ横断のビット一致は主張しない | `Math.sin` / `Math.cos` の結果は実装依存で、V8 と SpiderMonkey で最下位ビットが異なりうる。達成不能な閾値を掲げないため、主張を測れる範囲へ縮める |
| D-03 | ランクしきい値はステージ JSON の `score` を正本とし、既定値を原仕様 §24 の 900/700/400 に合わせる | §20(ステージごと)と §24(全体固定)が両立するよう、既定値を一致させて解消する |
| D-04 | 論理キャンバスを **900 × 540** とする | 原仕様の座標例(ゴール x=700,y=400, 60×60)が収まる最小の切りのよい寸法。表示は CSS で拡大縮小する |
| D-05 | ゴール滞在 300 ms を **18 tick**(60 Hz 固定)として数える | 固定ステップ(D-01)なので時間ではなく tick 数で数えられ、判定が決定論になる |
| D-06 | 物理コアは DOM に依存させず、Node で無頭実行できるようにする | G-01(解答可能性)と G-02(決定論)を単体テストで回すための前提。Matter.js 本体は DOM 非依存で、`Matter.Render` を使わなければ Node で動く |
| D-07 | Slow Motion(原仕様 §28)で `engine.timing.timeScale` を**動かさない**。代わりに「1 フレームで進める tick 数」を変える | matter-js 0.20 の `Engine.update` は `delta *= timing.timeScale` を行うので(実物で確認、2026-09-08)、timeScale を下げると**積分の刻みそのものが変わり、軌跡が変わる**。原仕様 §28 が求めているのは「同じものをゆっくり見る」ことであって「別の物理を回す」ことではない。刻みを固定したまま進める速さだけを変えれば、Slow Motion を挟んでも Replay(G-03)とスコア(§23)が壊れない |
| D-08 | 重力の範囲検査(原仕様 §55 の「gravity 0〜5」)を、**成分の大きさ**が 5 以下という意味に取る(`-5 ≤ gravityX, gravityY ≤ 5`) | 原仕様 §55 は符号を定めていない。一方 §22 の Stage 16「Wind = 重力 + 横方向力」は横風を要求しており、横風は左右どちらにも吹きうる。`gravityX ≥ 0` と読むと左向きの風が作れない |
| D-09 | スコアの経過秒を**切り捨てる**(原仕様 §23 の `elapsedSeconds × 5`) | 原仕様は丸め方を書いていない。秒は tick から導く連続量なので、決めておかないと同じ操作が別の点になる。切り捨てを採るのはプレイヤーに不利な方向へ勝手に丸めないため |
| D-10 | 原仕様 §22 の Stage 13 / 15 / 18 を、力ではなく**規定運動**(`mover`)で作る。位置と角度を tick の純関数として毎 tick 置き直す | 原仕様は「上下運動」「回転運動」「移動ターゲット」の作り方を定めていない。力で駆動すると初期条件と誤差の蓄積で運動が変わり、決定論(G-02)と解答可能性(G-01)が揺らぐ。tick の純関数なら状態が積み上がらず、何度走らせても同じ運動になる。置き直しは `updateVelocity = true` で行う —— 速度を伴わせないと台だけが動いてボールが取り残され、エレベータにならない |
| D-11 | 追加ボール(原仕様 §22 Stage 17)を入れても、**ゴール判定は主ボールだけ**を見る | どの球でもよいことにすると「多体衝突を利用して主ボールを運ぶ」という面の狙いが消え、「球をどれか一つ放り込む」面になる |

## 5. 検算の骨格(オラクル)

**「テスト全部緑」は「検査した」を意味しない。** 本プロジェクトが何を根拠に正しさを言うのかを、
主張の射程つきで先に決める。

| ID | オラクル | 主張すること | 主張**しない**こと |
|---|---|---|---|
| O-1 | **解答可能性** — 各ステージに保存した解答(部品配置列)を出荷物のエンジンで無頭実行し、制限時間内に CLEAR へ到達することを確かめる | 配るステージは**少なくとも一つの解を持つ** | 物理が現実に忠実であること。人間にとっての難易度。解が一意であること |
| O-2 | **決定論** — 同一ステージ・同一配置・同一シードで二度走らせ、全 tick の状態が一致することを確かめる | 同一 JS エンジン内で再現する | ブラウザ横断のビット一致(D-02) |
| O-3 | **リプレイ忠実度** — 操作ログの再生が、記録元と同じ最終状態・同じスコアを生む | Replay が記録の再現であること | 描画の一致 |
| O-4 | **保存量** — 空気抵抗 0・接触 0 件の自由飛行で力学的エネルギーが保たれることを確かめる | 散逸の無い世界で、積分が物理不変量を大きく破っていないこと | 厳密保存。下の「帳尻」の注を参照 |
| O-5 | **入力検証** — 範囲外・欠損・型違いのステージ JSON が Matter.js へ渡らないことを、陽性対照つきで確かめる | 検証器が実際に撃つこと | — |

**O-1 は循環していないか。** O-1 は出荷物のエンジンを使うので「物理が正しい」ことの証明にはならない。
しかし主張は「**このエンジンの下でこのステージは解ける**」であり、それはまさに配るときに必要な主張である。
健全性は片側にしか無い(解けると言えた面だけを配る。解けないと言うことはできない)。この非対称は明示して保つ。

**O-4 が独立である理由。** エネルギー保存はエンジンの実装から導いた式ではなく、外から持ち込んだ物理の不変量である。
エンジンの積分則を検査側で再実装すれば循環するが、保存量を測るだけなら循環しない。

**O-4 の残差は散逸ではなく帳尻である(実測 2026-09-08)。** matter-js の `body.velocity` は
Verlet の後退差分(tick n−1 → n の変位)なので、tick n の運動エネルギーは**半 tick ずれた速度**と
ずれていない位置とを組み合わせている。等加速度では `E_n = E_0 − a²n/2` という系統的なずれになり、
**窓の長さに比例して増える**。したがって閾値は「50 tick の窓」に対する値であって、窓を伸ばせば超える。
自由飛行 50 tick・接触 0 件・`frictionAir = 0` で相対変動 **0.0142**、
同じ窓で `frictionAir = 0.05` を入れると **0.179**(約 12.6 倍)。
**この差が、帳尻と本物の散逸を区別できることの根拠**であり、O-4 が主張してよいのはここまでである。
「厳密に保存する」とは言えない。

## 6. 品質ゲート

各ゲートは TEST_SPEC のケースから ID で参照されること。参照の無いゲートは検査で落とす(G-00)。

**状態欄の意味**(三値。HC-230):

- `実装済み` — そのゲートを確かめるテストが存在する
- `未実装(L#)` — テストも対象コードもまだ無い。予定ループを併記する
- `実装済み・テスト無し(L#)` — **対象コードは既に入っているがテストが無い**。
  予定ループを併記する

三つ目が要るのは、`実装済み` / `未実装` の二値では**割り当てのずれが表に出ない**からである。
G-07(ゴール滞在)は L2 に割り当てていたが、判定は物理ループの中で毎 tick 数えるほか無く、
L1 で `Simulation.step()` を書く手がそのまま書いてしまった。L2 でテストを書いた時点では
**もう緑**で、テスト先行の赤確認が構造的に成立しなかった。状態欄が `未実装(L2)` のまま
実態とずれていても、それを見る者がいなかった。

G-00 はこの三つのどれでもないゲートを落とす —— 宣言しただけのゲートを
「守られている」と読ませないための仕掛けである。

| ID | ゲート | 判定 | 状態 |
|---|---|---|---|
| G-00 | すべての `G-xx` が TEST_SPEC のケースから参照されるか、本 SPEC に「未実装」と明記されている | 例外があれば落とす | 実装済み |
| G-01 | 収録全ステージが保存解答で CLEAR に到達する(O-1) | 1 件でも未到達なら落とす | 実装済み |
| G-02 | 同一入力の二回実行が全 tick で一致する(O-2)。**摂動を入れた陽性対照が落ちること**も同時に確かめる | 不一致 or 対照が撃たなければ落とす | 実装済み |
| G-03 | Replay 再生が記録元と同じ最終状態・スコアを生む(O-3) | 不一致で落とす | 実装済み |
| G-04 | 無損失世界のエネルギー保存が閾値内(O-4) | 実測 0.0142 に対し閾値 0.03(50 tick の窓) | 実装済み |
| G-05 | ステージ JSON 検証が不正値を弾き、正常値を弾かない(O-5、陰性対照つき) | 誤検出・見逃しで落とす | 実装済み |
| G-06 | スコア計算が原仕様 §23 の式と一致し、ランク境界(§24)が境界値で正しい | 不一致で落とす | 実装済み |
| G-07 | ゴール判定が 18 tick 未満の通過を CLEAR にしない(§16) | 通過をクリアにしたら落とす | 実装済み |
| G-08 | アンロックと LocalStorage 保存・復元が往復する(§37, §38) | 往復で崩れたら落とす | 実装済み |
| G-09 | 全ステージが Bodies ≤ 100 / Constraints ≤ 30(N-02) | 超過で落とす | 実装済み |
| G-10 | 外部通信・外部素材・`dangerouslySetInnerHTML` を持たない(N-03, N-05) | 検出で落とす | 未実装(L6) |
| G-11 | LocalStorage の保存キーが `physicsPuzzleLab.v1` のみで、個人情報を含まない(N-04) | 逸脱で落とす | 実装済み |
| G-12 | アクセシビリティの静的要件(aria-label・タップ領域 44px・reduced-motion)を満たす | 未達で落とす | 未実装(L6) |
| G-13 | 日本語本文に別字種・制御文字が混入していない(N-07) | 検出で落とす | 未実装(L6) |
| G-14 | 実ブラウザ検品: 複数の画面幅で横溢れが無く、主要導線が到達可能 | 溢れ・不達で落とす | 未実装(L6) |

## 7. スコープ外(V2 以降 — 原仕様 §5)

ユーザー登録 / クラウド DB / オンラインランキング / SNS ログイン / マルチプレイヤー /
リアルタイム対戦 / ステージ投稿 / AI Solver / PyTorch 推論 / WebGPU 物理演算 /
流体シミュレーション / 3D 物理演算。

V1.1 以降の候補(原仕様 §72–§74)も本バージョンでは作らない。

## 8. 用語

- **tick** — 固定タイムステップ 1 回分(1000/60 ms)。時間の単位はすべて tick で数える(D-05)
- **配置(Placement)** — プレイヤーが置いた部品 1 個の位置・角度・種別
- **解答(Solution)** — あるステージを CLEAR させる配置の集合。ステージに同梱し G-01 で検査する
